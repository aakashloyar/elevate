info:
  name: create-user
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/users
  body:
    type: json
    data: |-
      {
        "username": "aakashloyar",
        "email": "aakashloyar@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
