info:
  name: create-user
  type: http
  seq: 2

http:
  method: POST
  url: http://ec2-43-204-230-140.ap-south-1.compute.amazonaws.com:8080/users
  body:
    type: json
    data: |-
      {
        "username": "krishanloyar",
        "email": "krishanloyar@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
