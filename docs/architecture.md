# architecture 
-> golang for programming
-> postgres for database
-> gemini for llm
-> sql for queries
-> ec2 for hosting 
-> s3 for file storage 
-> next/react for web frontend


# Services 
1.  User Service 
2.  Exam Create Service 
3.  Exam Runner Service 
4.  Submission Service
5.  Evaluation Service 
6.  Auth Service 
7.  Document/ RAG Service 
8.  Search Service 
9.  Analytics Service 
10. Notification Service 

-> Till service 6 we will work 
-> other services are optional

-> document service we will do later


-> now here comes the the architecture 
-> now as we want our problems to be reusable across assessment
-> this descision is taken so that we can reuse the problems also in the new assessment
-> and problems are not bounded to specific assessment
-> now the problems you can create manually also 
-> they can also be generated by ai 
-> so in the problem we are not storing assessmentid
-> instead we have a specific problemid and assesmentid which will be handled by assessment service
-> now here comes the thing
-> suppose user is adding a problem manually 
-> then how it works
-> now we are not giving user a functionality to add problems without any assessment
-> user can add problem with a assessment only 
-> so how a manuall problem will get pushed to db
-> the flow that we are thinking is client put the problem then get the problem id
-> then it will push problem id and assessment id to assessment 
-> now is this correct
-> no as suppose client got disconnected for 2nd request so not a good way
-> 2nd way can be bff 
-> but ya 1 thing we can do is
-> there will /add route in assessment which will just take the problem send the problem to problem service 
-> get the problemid and now store it in problemid assessmentid table
-> but how the problem is sent to problem service 
-> should we go with synchrounous way or asynchronous way
-> like directly doing through http
-> or doing it through kafka

-> now for simple manual problem adding
-> we will go with http as it does not take long time 
-> so it is good that user also get to know that his question is acccepted or not

-> but for ai generation service we will use asynchronous kafka
-> as it will take time 
-> user will get notified after everything is done

-> ok now here how the flow work
-> generation service will create the problems 
-> it will push each problem 1 by 1 to problem service 
-> but who will notify assessment service 
-> should it be this way 
-> ki from generation service it must be going to assessment service 
-> so that assessment service can handle this 
-> such that it will push the problem to problem service
-> and problem service will send it back the id and it can update
-> now here comes the next doubt 
-> now should assessment to problem communication should work by http or kafka 
-> also generation to assessment work by http or kafka
-> also should we just go with generation to problem directly

-> how to select between sync vs async communication
-> 1 main thing that matters is if it require response now only
-> or it does not matter to who is sending
-> let us take example of auth service
-> everyone request would need to authenticated by auth service
-> so there is high load on it 
-> so should we go with async 
-> not right becuase it want instant response
-> so we need to go with sync
-> and auth service need to be scaled up
-> also 1 more misconception is auth service do everything
-> like it authenticate everytime
-> no but it handles only login, logout
-> token providing, refreshing

-> so how authentication happens
-> it happens using private/public keys
-> auth service has it's private key 
-> means auth_secret
-> and the public key each service will have 
-> using the public key each service will decrypt the token 


-> i just wanted to understand which service handle 
-> that when user wanted to see it's submission 
-> and check which option was correct or incorrect

```
Submission Service → what the user answered
Evaluation Service → whether it was correct, marks, score, etc.
Problem Service → the original question and its correct option(s)
```