info:
  name: list-problems
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/problems?type=single
  params:
    - name: type
      value: single
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
