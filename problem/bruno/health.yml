info:
  name: health
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8083/problem/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
