after taking generation request it will produce to kafka so that llm service can consume 
-> in kafka event it will send the complete prompt and metadata like jobid userid

after this it will consume the result generated by llm service

also this service will produce events to problem and assessment service after question generated


