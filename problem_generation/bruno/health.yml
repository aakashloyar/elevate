info:
  name: health
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8087/problem-generation/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
